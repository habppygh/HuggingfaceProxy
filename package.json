{
  "name": "hf-proxy",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "dev": "npm run build && wrangler pages dev .",
    "deploy": "npm run build && wrangler pages deploy ."
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "wrangler": "^3.0.0"
  }
}
